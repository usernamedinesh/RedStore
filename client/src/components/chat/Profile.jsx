// while chatting then
// uses can see each other profile
// just an details with name and image and bio
function Profile() {
  return (
    <div>
      <h1>profile page</h1>
    </div>
  );
}

export default Profile;
